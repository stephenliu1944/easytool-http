import HttpRequest, { proxyBaseURL } from '../src/index';

describe('ReturnType is promise', function () {
    it('xxx', function () {});
});

describe('ReturnType is url', function () {
    it('xxx', function () {});
});